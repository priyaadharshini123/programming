Given an integer n, calculate the sum of series 1^3 + 2^3 + 3^3 + 4^3 + … till n-th term.


class Solution {
  public:
    int sumOfSeries(int n) {
        
          int sum=0;
          for(int i=1;i<=n;i++){
              sum=sum+(i*i*i);
          }
          return sum;
    }
};
---------------------------------------------------------------------------------------------------------------------------------------------------------
Input: n = 7
Output: 784.
